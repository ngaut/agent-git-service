# Use gh as a credential helper
exec gh auth setup-git

# Create a repository with a file so it has a default branch
exec gh repo create $ORG/$SCRIPT_NAME-$RANDOM_STRING --add-readme --private

# Defer repo cleanup
defer gh repo delete --yes $ORG/$SCRIPT_NAME-$RANDOM_STRING

# Clone the repo
exec gh repo clone $ORG/$SCRIPT_NAME-$RANDOM_STRING
cd $SCRIPT_NAME-$RANDOM_STRING

# Create a release (this also creates the tag)
exec gh release create v1.0.0 --notes 'release to delete'

# Verify the release exists
exec gh release view v1.0.0
stdout 'v1.0.0'

# Delete the release and its tag
exec gh release delete v1.0.0 --yes --cleanup-tag

# Verify the release is gone
! exec gh release view v1.0.0
